{
  "name": "iit-project-management",
  "private": true,
  "packageManager": "pnpm@11.19.0",
  "scripts": {
    "dev": "tsc -p apps/server/tsconfig.json && concurrently -n server,web -c blue,green \"node apps/server/dist/main.js\" \"vite apps/web --config apps/web/vite.config.ts\"",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "demo:seed": "node scripts/seed-showcase.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0",
    "vite": "^7.0.0"
  }
}
